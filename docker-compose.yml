services:
  postgres:
    image: postgres:18-alpine
    container_name: postgres
    ports:
      - 6500:5432
    env_file:
      - ./.env.server
    volumes:
      - postgres:/var/lib/postgresql
volumes:
  postgres: